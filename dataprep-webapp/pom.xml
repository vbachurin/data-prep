<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>org.talend.dataprep</groupId>
    <artifactId>dataprep-webapp</artifactId>
    <version>1.0.m1-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>dataprep-webapp</name>

    <prerequisites>
        <maven>3.1.0</maven>
    </prerequisites>
        <properties>
            <docker.registry.name>talend-registry:5000</docker.registry.name>
            <docker.tools.image.name>${docker.registry.name}/talend/data-prep-webapp-tools:1.0</docker.tools.image.name>
<!--            <docker.registry.name>tal-qa158.talend.lan:5000</docker.registry.name>-->
        </properties>
    <build>
        <plugins>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>1.3.2</version>
                <executions>
                    <execution>
                        <id>docker pull ${docker.tools.image.name}</id>
                        <goals>
                            <goal>exec</goal>
                        </goals>
                        <phase>pre-clean</phase>
						   <configuration>
							  <executable>docker</executable>
							  <arguments>
								<argument>pull</argument>
								<argument>${docker.tools.image.name}</argument>
							  </arguments>
							</configuration>
                    </execution>
                    <execution>
                        <id>npm prune and bower prune</id>
                        <goals>
                            <goal>exec</goal>
                        </goals>
                        <phase>clean</phase>
						   <configuration>
							  <executable>docker</executable>
							  <arguments>
								<argument>run</argument>
								<argument>-v</argument>
								<argument>${basedir}:/dataprep-webapp</argument>
								<argument>${docker.tools.image.name}</argument>
								<argument>/bin/sh </argument>
								<argument>-c</argument>
								<argument>npm prune &amp;&amp; bower --allow-root --config.interactive=false prune</argument>
							  </arguments>
							</configuration>
                    </execution>
                    <execution>
                        <id>npm install and bower install</id>
                        <goals>
                            <goal>exec</goal>
                        </goals>
                        <phase>initialize</phase>
						   <configuration>
							  <executable>docker</executable>
							  <arguments>
								<argument>run</argument>
								<argument>-v</argument>
								<argument>${basedir}:/dataprep-webapp</argument>
								<argument>${docker.tools.image.name}</argument>
								<argument>/bin/sh </argument>
								<argument>-c</argument>
								<argument>npm install &amp;&amp; bower --allow-root --config.interactive=false install</argument>
							  </arguments>
							</configuration>
                    </execution>
                    <execution>
                        <id>gulp test:ci</id>
                        <goals>
                            <goal>exec</goal>
                        </goals>
                        <phase>test</phase>
						   <configuration>
							  <executable>docker</executable>
							  <arguments>
								<argument>run</argument>
								<argument>-v</argument>
								<argument>${basedir}:/dataprep-webapp</argument>
								<argument>${docker.tools.image.name}</argument>
								<argument>/bin/sh </argument>
								<argument>-c</argument>
								<argument>gulp test:ci</argument>
							  </arguments>
							</configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>full-clean</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-clean-plugin</artifactId>
                        <version>2.4.1</version>
                        <configuration>
                            <filesets>
                                <fileset>
                                    <directory>node_modules</directory>
                                    <followSymlinks>false</followSymlinks>
                                </fileset>
                                <fileset>
                                    <directory>bower_components</directory>
                                    <followSymlinks>false</followSymlinks>
                                </fileset>
                                <fileset>
                                    <directory>node</directory>
                                    <followSymlinks>false</followSymlinks>
                                </fileset>
                            </filesets>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>
